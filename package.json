{
  "name": "infra-monitor",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "npm run dev -w apps/monitoring-api",
    "dev:dashboard": "npm run dev -w apps/dashboard",
    "dev:agent": "npm run dev -w apps/agent",
    "build": "npm run build -w packages/shared && npm run build -w apps/monitoring-api && npm run build -w apps/dashboard"
  }
}
